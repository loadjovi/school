(()=>{
  if(typeof state==="undefined")return;
  state.schoolAccessAudit=state.schoolAccessAudit||{loaded:false,loading:false,stats:[],error:""};
  const fmt=v=>{if(!v)return "尚無紀錄";try{return new Intl.DateTimeFormat("zh-TW",{timeZone:"Asia/Taipei",year:"numeric",month:"2-digit",day:"2-digit",hour:"2-digit",minute:"2-digit",hour12:false}).format(new Date(v))}catch{return v}};
  async function loadAudit(force=false){if(state.me?.role!=="admin"||state.schoolAccessAudit.loading)return;if(state.schoolAccessAudit.loaded&&!force)return;state.schoolAccessAudit.loading=true;state.schoolAccessAudit.error="";try{const d=await api("/api/school-access");state.schoolAccessAudit.stats=d.accessStats||[];state.schoolAccessAudit.loaded=true}catch(e){state.schoolAccessAudit.error=e.message||String(e)}state.schoolAccessAudit.loading=false;mountAudit()}
  function rows(){const a=state.schoolAccessAudit.stats||[];if(!a.length)return `<div class="notice">尚未設定校方查詢 Gmail，或目前尚無使用紀錄。</div>`;return a.map(x=>`<div class="item" style="display:block"><div style="display:flex;justify-content:space-between;gap:8px"><b>${esc(x.email)}</b><span class="badge ${x.lastLoginAt?"ok":"warn"}">${x.lastLoginAt?"已使用":"未使用"}</span></div><small style="display:block;margin-top:6px">最近登入：<b>${esc(fmt(x.lastLoginAt))}</b><br>最近匯出當日出缺勤：<b>${esc(fmt(x.lastAttendanceExportAt))}</b><br>${esc(x.month||"")} 登入：<b>${x.monthlyLoginCount||0} 次</b>｜匯出：<b>${x.monthlyAttendanceExportCount||0} 次</b></small>${(x.monthlyLogins||[]).length?`<details style="margin-top:8px"><summary>查看本月登入時間</summary><div class="notice" style="margin-top:6px">${x.monthlyLogins.map(t=>esc(fmt(t))).join("<br>")}</div></details>`:""}${(x.monthlyAttendanceExports||[]).length?`<details style="margin-top:8px"><summary>查看本月匯出紀錄</summary><div class="notice" style="margin-top:6px">${x.monthlyAttendanceExports.map(e=>`${esc(fmt(e.at))}｜資料日期：${esc(e.date||"—")}`).join("<br>")}</div></details>`:""}</div>`).join("")}
  function html(){const s=state.schoolAccessAudit;if(s.error)return `<div class="card"><h2>📊 校方系統使用月報</h2><div class="error">讀取失敗：${esc(s.error)}</div></div>`;if(s.loading&&!s.loaded)return `<div class="card"><h2>📊 校方系統使用月報</h2><div class="notice">正在讀取使用紀錄…</div></div>`;return `<div class="card"><div class="section-title"><h2>📊 校方系統使用月報</h2><button class="secondary" style="margin:0;width:auto" onclick="refreshSchoolAccessAudit()">重新整理</button></div><div class="notice">登入代表校方帳號成功進入系統；「匯出」僅在校方實際點選匯出當日出缺勤 CSV 時記錄。</div>${rows()}</div>`}
  function mountAudit(){if(state.me?.role!=="admin"||state.page!=="admin")return;const host=document.getElementById("schoolAccessAdmin")||document.querySelector(".main");if(!host)return;let root=document.getElementById("schoolAccessAudit");if(!root){root=document.createElement("div");root.id="schoolAccessAudit";host.insertAdjacentElement("afterend",root)}root.innerHTML=html()}
  function moveSchoolExportButton(){if(state.me?.role!=="school"||state.page!=="school")return;const btn=[...document.querySelectorAll("button")].find(x=>String(x.textContent||"").includes("匯出當日出缺勤 CSV"));if(!btn)return;const card=btn.closest(".card");const title=card?.querySelector(".section-title");if(card&&title&&title.nextElementSibling!==btn){btn.style.marginTop="12px";btn.style.marginBottom="12px";title.insertAdjacentElement("afterend",btn)}}
  window.refreshSchoolAccessAudit=async()=>{state.schoolAccessAudit.loaded=false;await loadAudit(true)};
  const originalExport=window.exportSchoolFollowup;
  if(typeof originalExport==="function")window.exportSchoolFollowup=function(){const d=state.schoolViewer?.data;originalExport();if(state.me?.role==="school"&&d?.date)api("/api/school-attendance-export-audit",{method:"POST",body:JSON.stringify({date:d.date})}).catch(e=>console.warn("school export audit failed",e?.message||e))};
  const baseRender=render;render=function(){const r=baseRender();if(state.me?.role==="admin"&&state.page==="admin")setTimeout(()=>{mountAudit();loadAudit()},0);if(state.me?.role==="school"&&state.page==="school")setTimeout(moveSchoolExportButton,0);return r};
  if(state.me?.role==="admin")setTimeout(()=>loadAudit(),0);
  if(state.me?.role==="school")setTimeout(moveSchoolExportButton,0);
})();